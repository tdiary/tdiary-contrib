language: ruby
cache: bundler

rvm:
  - 2.4.7
  - 2.5.7
  - 2.6.5
  - ruby-head

before_install:
  - gem update bundler

script: bundle exec rake spec

notifications:
  irc: "irc.freenode.org#tdiary"

matrix:
  allow_failures:
    - rvm: ruby-head
